{
  "name": "xiaoy-calendar",
  "private": true,
  "version": "1.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:cloudflare": "npm run build && node scripts/stage-cloudflare-apk.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "android:sync": "npm run build && node scripts/prepare-android-assets.mjs && cap sync android",
    "android:apk": "npm run android:sync && cd android && gradlew.bat assembleDebug",
    "android:release": "npm run android:sync && cd android && gradlew.bat assembleRelease bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "lunar-javascript": "^1.7.7",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
